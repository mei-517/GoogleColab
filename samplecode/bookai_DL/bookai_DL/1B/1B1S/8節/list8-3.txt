from sklearn.cluster import KMeans

# データセットを2グループに分割
kcls = KMeans(n_clusters=2)
group = kcls.fit_predict(biscuit[['高さ', '幅']])

# 各データセットのグループIDを確認
print(group)

# データセットにグループIDを結合
biscuit['グループ'] = group
biscuit.head()
