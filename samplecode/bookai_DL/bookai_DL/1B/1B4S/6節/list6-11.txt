min_aic = 10000
min_p = min_q = min_d = min_P = min_Q = min_D = -1
for D in range(2):
    for P in range(2):
        for Q in range(2):
            for d in range(2):
                for p in range(4):
                    for q in range(3):
                        result = SARIMAX(y, order=(p, d, q), seasonal_order=(P,D,Q,31), trend='n').fit()
                        if min_aic > result.aic:
                            min_aic = result.aic
                            min_p = p
                            min_q = q
                            min_d = d
                            min_P = P
                            min_Q = Q
                            min_D = D
print('-----')
print(min_p, min_d, min_q, min_P, min_D, min_Q, min_aic)